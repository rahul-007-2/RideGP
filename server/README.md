Server for RideGP

Setup:

1. Copy `.env.example` to `.env` and fill `SUPABASE_SERVICE_ROLE_KEY` and `PORT`.
2. Install deps and run:

```bash
npm install
npm run dev
```

Endpoints:
Requires: `SUPABASE_URL` and `SUPABASE_SERVICE_ROLE_KEY` in `.env`.
